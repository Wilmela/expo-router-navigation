import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  text: { fontSize: 20, fontWeight: "400", marginBottom: 2 },
  headingText: {
    fontSize: 30,
    color: "#222",
    fontWeight: "bold",
  },
});
